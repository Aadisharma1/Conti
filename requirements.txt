# pin for reproducibility; bump intentionally after verifying experiments.
torch>=2.1.0,<3
transformers>=4.38.0,<5
accelerate>=0.27.0,<1
datasets>=2.16.0,<3
peft>=0.10.0,<1
safetensors>=0.4.0
pyyaml>=6.0.1
tqdm>=4.66.0
numpy>=1.26.0,<3
# optional — only needed for model-based verifier + plotting
# matplotlib>=3.8.0
# wandb>=0.16.0
